# Name of the build space
BUILDSPACE_NAME="x86-64bit-workstation"

# Email address of the build-space administrator, if not defined any emails
# will be sent to root (or their local alias).
# ADMIN_EMAIL="build.admin@example.com"

# Base path for logs
LOGPATH="/var/log/autobuild"

# List of configuration files to protect from updates.
CONFFILES="aliases bashrc buildspace clock hosts layman.cfg locale.gen make.conf resolv.conf ssmtp.conf"

# List of kernel sources which should not be automatically cleaned.
KEEP_KERNELS="sys-kernel/gentoo-sources"

# List of packages which should not be automatically cleaned.
KEEP_PACKAGES="dev-lang/python sys-devel/gcc" 